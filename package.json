{
  "name": "skillbox",
  "displayName": "SkillBox",
  "description": "VSCode extension for managing Agent Skills from subscription sources",
  "version": "0.0.1",
  "publisher": "leonezhu",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:skillbox"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "skillbox",
          "title": "SkillBox",
          "icon": "resources/skillbox.svg"
        }
      ]
    },
    "views": {
      "skillbox": [
        {
          "id": "skillbox.sources",
          "name": "Sources"
        }
      ]
    },
    "commands": [
      {
        "command": "skillbox.addSource",
        "title": "Add Source",
        "icon": "$(add)"
      },
      {
        "command": "skillbox.refreshSources",
        "title": "Refresh Sources",
        "icon": "$(refresh)"
      },
      {
        "command": "skillbox.syncSource",
        "title": "Sync This Source"
      },
      {
        "command": "skillbox.removeSource",
        "title": "Remove This Source"
      },
      {
        "command": "skillbox.installSkill",
        "title": "Install Skill"
      },
      {
        "command": "skillbox.updateSkill",
        "title": "Update Skill"
      },
      {
        "command": "skillbox.openSettings",
        "title": "Open Settings",
        "icon": "$(gear)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "skillbox.addSource",
          "when": "view == skillbox.sources",
          "group": "navigation"
        },
        {
          "command": "skillbox.refreshSources",
          "when": "view == skillbox.sources",
          "group": "navigation"
        },
        {
          "command": "skillbox.openSettings",
          "when": "view == skillbox.sources",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "skillbox.syncSource",
          "when": "view == skillbox.sources && viewItem == source",
          "group": "1_sync"
        },
        {
          "command": "skillbox.removeSource",
          "when": "view == skillbox.sources && viewItem == source",
          "group": "2_manage"
        },
        {
          "command": "skillbox.installSkill",
          "when": "viewItem == skill",
          "group": "1_install"
        },
        {
          "command": "skillbox.updateSkill",
          "when": "viewItem == skill-installed",
          "group": "1_update"
        }
      ]
    },
    "configuration": {
      "title": "SkillBox",
      "properties": {
        "skillbox.centralRepo": {
          "type": "string",
          "default": "",
          "description": "Central repository path for storing synced skills (leave empty to use VSCode global storage)"
        },
        "skillbox.defaultAgent": {
          "type": "string",
          "enum": ["copilot", "openclaw", "claude", "cursor"],
          "enumDescriptions": [
            "GitHub Copilot - skills to .github/skills",
            "OpenClaw - skills to .agents/skills",
            "Claude - skills to .claude/skills",
            "Cursor - skills to .cursor/skills"
          ],
          "default": "copilot",
          "description": "Default agent type for skill installation"
        },
        "skillbox.defaultScope": {
          "type": "string",
          "enum": ["project", "global"],
          "enumDescriptions": [
            "Install to current workspace",
            "Install to user home directory"
          ],
          "default": "project",
          "description": "Default installation scope"
        },
        "skillbox.installMethod": {
          "type": "string",
          "enum": ["copy", "symlink"],
          "enumDescriptions": [
            "Copy skill files to target location",
            "Create symbolic link (requires admin privileges on some systems)"
          ],
          "default": "copy",
          "description": "Method for installing skills"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^7.x",
    "@typescript-eslint/parser": "^7.x",
    "eslint": "^8.x",
    "typescript": "^5.x"
  },
  "dependencies": {
    "simple-git": "^3.x"
  }
}
